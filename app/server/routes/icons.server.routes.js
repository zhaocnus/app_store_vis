'use strict';

module.exports = function(app) {
	var icons = require('../../server/controllers/icons');



/*
	// Scores Routes
	app.route('/scores')
		.get(scores.list)
		.post(scores.create);

	//app.route('/addDefault')
	//	.get(scores.addDefault);

	// Scores Routes
	app.route('/scoresTime')
		.get(scores.listTimes)
		.post(scores.create);

	app.route('/csvExport')
		.get(scores.csvExport);

	app.route('/deleteall')
		.get(scores.deleteAll);

	app.route('/updateFilterboardName')
    .post(scores.updateFilterboardName);


	app.route('/scores/:scoreId')
		.get(scores.read)
		.put(scores.update)
	  .delete(scores.delete);

	// Finish by binding the Score middleware
	app.param('scoreId', scores.scoreByID);
*/

};
